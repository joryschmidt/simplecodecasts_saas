<div class='jumbotron text-center'>
  <h1>Welcome to Dev Match</h1>
  <h3>A membership site where entreprenuers can meet developers</h3>
</div>
<div class='row'>
  <% if user_signed_in? %>
    <div class='col-md-10 text-center'>
      <div class='well'>
        <h2> The Community </h2>
        <p>Visit the community to connect with other like-minded folks.</p><br />
        <%= link_to 'Visit The Community', '#', class: 'btn btn-primary btn-lg btn-block' %>
      </div>
    </div>
    <div class='col-md-2'>
      <%= link_to 'Create Your Profile', new_user_profile_path(current_user), class: 'btn btn-primary btn-lg' unless current_user.profile %>
      <% unless current_user.profile %><br /><br /><p class='text-center'><b>YOU SIMPLY MUST!</b></p><% end %>
      <% if current_user.profile %>
        <br />
        <%= link_to 'Profile', user_path(current_user), class: 'btn btn-success btn-lg'%>
        <%= link_to 'Edit Profile', edit_user_profile_path(current_user), class: 'btn btn-primary btn-lg' %>
        <%= link_to 'Delete Profile', '#', class: 'btn btn-danger btn-lg' %>
      <% end %>
    </div>
  <% else %>
    <div class='col-md-6'>
      <div class='well'>
        <h2 class='text-center'>Basic Membership</h2>
        <h4 class='text-center'>Sign up for free and get basic access to the community.</h4>
        <br />
        <%= link_to 'Sign Up with Basic', new_user_registration_path(plan: @basic_plan.id), class: 'btn btn-primary btn-lg btn-block' %>
      </div>
    </div>
    <div class='col-md-6'>
      <div class='well'>
        <h2 class='text-center'>Pro Membership</h2>
        <h4 class='text-center'>Sign up for the pro account and get exclusive access to the contact information of other members for only $10/month!</h4>
        <%= link_to 'Sign Up with Pro', new_user_registration_path(plan: @pro_plan.id), class: 'btn btn-success btn-lg btn-block' %>
      </div>
    </div>
  <% end %>
</div>